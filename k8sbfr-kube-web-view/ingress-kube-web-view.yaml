apiVersion: networking.k8s.io/v1
#apiVersion: v1
kind: Ingress
metadata:
  name: k8sbfr-kubwebview
spec:
  ingressClassName: nginx
  rules:
    - host: k8sbfr.zapto.org
      http:
        paths:
          - path: /kubewebview
            pathType: Prefix
            backend:
              service:
                name: kube-web-view
                port: 
                  number: 80